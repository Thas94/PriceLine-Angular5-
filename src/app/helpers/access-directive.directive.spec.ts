import { AccessDirectiveDirective } from './access-directive.directive';

describe('AccessDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
